---
import Layout from "../layouts/Layout.astro";
import { GenerateScreenContainer } from "../components/GenerateScreenContainer";

// TODO: Add ProtectedRoute logic when authentication is implemented
// For now, this is a public page

// const user = Astro.locals.user;
// if (!user) {
//   return Astro.redirect("/login");
// }

export const prerender = false;
---

<Layout title="Generate Flashcards | Natigo">
  <main class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto">
      <h1 class="text-3xl font-bold mb-2">Generate Flashcards</h1>
      <p class="text-gray-600 dark:text-gray-400 mb-8">
        Paste your text and let AI create flashcards for you
      </p>
      
      <!-- React container with client:load directive -->
      <GenerateScreenContainer client:load />
    </div>
  </main>
</Layout>
